/**
 * Simplified i18n module (single language mode)
 *
 * This template uses English only by default.
 * For multi-language support, see docs/ADVANCED_I18N.md
 */

export const DEFAULT_LOCALE = 'en';
export type Locale = 'en';
